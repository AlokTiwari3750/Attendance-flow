import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.orgaearth.attendance',
  appName: 'Attendance Flow',
  webDir: 'dist'
};

export default config;
